## 更新内容

- 投影 / 外发光 / 模糊 增加「启用」开关，关闭即不生效
- 高级编辑区新增「恢复默认」按钮
- 选中带 effects 的预设时，高级编辑区自动禁用并提示
- 弹幕模式：无效起始时间不再静默回退
- 发送成功提示的时间与真实发送位置一致（含时间偏移）
- 发送后自动清空起始时间
- 运动轨迹旋转超过一圈时显示「N圈M°」换算
- 弹幕模式隐藏无意义的「样式来源」下拉
- 运动轨迹默认折叠拉伸/旋转高级项
- 预设「恢复默认」对带 effects 的预设也显示
- 「保存参数」一并持久化 effects
- 效果开发面板保存的预设自带参数声明，保存后可在预设面板直接微调
- 开发面板草稿（引擎组合/参数/预览文本）自动持久化，关面板或刷新后还原
- 开发面板新增「↺ 重置」按钮，一键清回初始组合
- 预设导出改为弹窗补全命名/描述/作者后导出 JSON；作者默认自动填当前 A 站昵称 + uid，效果开发面板保存的预设也会自动署名
- 预设导入校验参数：缺 key / 重复 / type 拼错 / 引擎未声明时给提醒
- 参数 key 命名约定：新引擎带前缀防冲突，跨阶段联动参数才用裸名（见 50-engine.js 注释）
- 代码重构：源码拆分为 13 个模块 + 构建脚本（node build.js）
- ASS 字幕多行对白（`{\N}` 硬换行）不再只取首行，保留全部行
- 字幕列表 / 预设名称做 HTML 转义，防止下载他人字幕或预设时注入脚本
- 版本号改为单一来源：运行时读 `@version`，构建时校验 header 与 package.json 一致
