## 更新内容

- 新增「离线预览」：Canvas 自绘，不依赖 A 站渲染器，默认开启，可在预览栏取消勾选切回在线（复用 A 站渲染器）
- 离线预览完整支持 cubic-bezier 缓动、多行字幕（SRT/ASS 的换行）、高清屏（retina）清晰渲染
- 一句字幕展开成多条弹幕时加节流，避免瞬间连发触发限流；发送确认弹窗显示真实弹幕条数
- 取消发送降到弹幕级；中途取消的句子如实标记「仅发送部分，重发会重复已发部分」
- 修复 ASS 颜色带尾随 `&`（如 `&H000000FF&`）被静默解析成白色
- 修复扫光时序 `step.time` 清空后扫光被静默关闭
- 修复超长字幕截断劈开 emoji、验证时长兜底导致大量请求、`lastSentIds` 跨批次累积、超时重发无提示等问题
- 预览可通过「↺ 重置」「🗑 移除」或切换预览方式清除，无需刷新页面
